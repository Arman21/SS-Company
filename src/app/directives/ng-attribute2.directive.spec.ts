import { NgAttribute2Directive } from './ng-attribute2.directive';

describe('NgAttribute2Directive', () => {
  it('should create an instance', () => {
    const directive = new NgAttribute2Directive();
    expect(directive).toBeTruthy();
  });
});
